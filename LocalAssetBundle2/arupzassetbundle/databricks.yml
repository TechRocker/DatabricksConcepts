# This is a Databricks asset bundle definition for arupzassetbundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: arupzassetbundle
  uuid: e488535b-2fcd-441b-a9b8-c7c83c8ab7cc

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-4814c37b-e52c.cloud.databricks.com
    variables:
      catalog: circuitbox
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-4814c37b-e52c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/connect.to.arup@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/connect.to.arup@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: circuitbox
      schema: prod
    permissions:
      - user_name: connect.to.arup@gmail.com
        level: CAN_MANAGE
