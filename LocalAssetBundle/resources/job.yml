resources:
  jobs:
    demo-job:
      name: demo-job
      tasks:
        - task_key: run-notebook
          spark_python_task:
            python_file: ../src/main.py    
          environment_key: default    

      environments:
        - environment_key: default
          spec:
            serverless: true
            
            environment_version: "4"